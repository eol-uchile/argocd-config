name: Update portal-eol version

on:
  repository_dispatch:
    types: [update-portal]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Create GitHub App Token
        id: app_token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.EOLITO_BOT_APP_ID }}
          private-key: ${{ secrets.EOLITO_BOT_PRIVATE_KEY }}
          owner: eol-uchile
          repositories: argocd-config

      - uses: actions/checkout@v4
        with:
          ref: 'main'
          token: ${{ steps.app_token.outputs.token }}

      - name: Install kustomize
        uses: imranismail/setup-kustomize@v2.1.0
        with:
          kustomize-version: 5.7.0

      - name: Update portal-eol version
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
        run: |
          cd portal-eol
          NEWVERSION=$(gh api --header "Accept: application/vnd.github+json" --header "X-GitHub-Api-Version: 2022-11-28" /orgs/eol-uchile/packages/container/portal-eol/versions | jq '.[0] | .metadata.container.tags[]' | sed 's#"##g')
          kustomize edit set image portal=ghcr.io/eol-uchile/portal-eol:$NEWVERSION
          cd ..

      - name: Push changes
        run: |
          if [ "$(git status --porcelain portal-eol/)" != "" ]
          then
            git config --global user.name "eolito[bot]"
            git config --global user.email "217098583+eolito[bot]@users.noreply.github.com"
            git add portal-eol/kustomization.yaml
            git commit -m "Update portal-eol version"
            git push HEAD:main
          else
            echo "No changes to commit. Skipping commit step."
          fi
