---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: moodle4-dev-cron
  annotations:
    reloader.stakater.com/auto: "true"
  name: moodle4-dev-cron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: moodle4-dev-cron
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: moodle4-dev-cron
    spec:
      nodeSelector:
        kubernetes.io/hostname: cloud-elearning01
      containers:
        - image: ghcr.io/eol-uchile/moodle-vlabx-docker:f009d37f78855fcc45df5435e4d0af59dfbd537b
          name: moodle-cron
          command: ["/bin/sh"]
          args: ["-c", "trap exit TERM; while :; do php /var/www/html/admin/cli/cron.php; sleep 1m & wait $${!}; done;"]
          volumeMounts:
            - mountPath: /var/www/html/config.php
              name: config
              subPath: moodle.php
            - mountPath: /tmp
              name: tmpfs
            - mountPath: /var/www/moodledata
              name: data
          securityContext:
            readOnlyRootFilesystem: true
      volumes:
        - name: tmpfs
          emptyDir: {}
        - name: config
          secret:
            secretName: moodle4-dev-config
        - name: data
          hostPath:
            path: /home/eol/virtuallabx/moodle/data
            type: Directory
