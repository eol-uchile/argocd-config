---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: metabase-backup-daily
spec:
  schedule: "30 7 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 4
  failedJobsHistoryLimit: 4
  jobTemplate:
    spec:
      backoffLimit: 1
      ttlSecondsAfterFinished: 432000 # 5 días
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: metabase-backup
              image: backup-container
              args:
                - /root/scripts/backup.sh
                - daily
                - postgresql
              envFrom:
                - secretRef:
                    name: metabase
                - secretRef:
                    name: common
          tolerations:
            - key: "backups"
              operator: "Equal"
              value: "true"
              effect: "NoSchedule"
