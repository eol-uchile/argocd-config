---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: znuny-backup-daily
spec:
  schedule: "0 8 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 4
  failedJobsHistoryLimit: 4
  jobTemplate:
    spec:
      backoffLimit: 1
      ttlSecondsAfterFinished: 432000 # 5 días
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: znuny-backup
              image: old-backup-container
              args:
                - /root/scripts/backup.sh
                - daily
                - postgresql
              envFrom:
                - secretRef:
                    name: znuny
                - secretRef:
                    name: common
          tolerations:
            - key: "backups"
              operator: "Equal"
              value: "true"
              effect: "NoSchedule"
